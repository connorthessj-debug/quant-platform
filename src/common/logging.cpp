#include "common/logging.h"

// Logger is fully implemented in the header (inline in the class body).
// This translation unit ensures the header compiles independently.

namespace qp {
// Intentionally empty — Logger implementation is header-only.
} // namespace qp
