#include "common/types.h"

// Types are header-only (all inline/struct definitions).
// This translation unit exists to ensure the header compiles independently
// and to provide a link target for the qp_common library.

namespace qp {
// Intentionally empty — all type definitions are in types.h
} // namespace qp
